bot:
  prefix: "$py"
  token: "PUT YOUR TOKEN HERE"
  intents:
    guilds: true
    messages: true

cogs:
  code_runner: enable

logging:
  format: "%(asctime)s: %(levelname)-9s %(name)-16s :: %(message)s"
  date_format: "%m/%d/%Y %I:%M:%S %p"

  loggers:
    Bot: INFO
    Bot.CodeRunner: DEBUG

